import { createContext } from 'react'

export const ServerURL = createContext('/server');

export const ShelfURL = createContext('/shelf');

export const Category = createContext();

export const Supreme = createContext(0);

export const PageNo = createContext(0);